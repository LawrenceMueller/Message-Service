import React, { Component } from "react";
import "./howItWorks_styles.css";

export default class HowItWorks extends Component {
  render() {
    return (
      <div className="how-it-works-container">
        <h3>How it Works</h3>
        <p>
          Simply sign yourself up or surprise a friend with a gift. Select the
          number of months (for simplicity each month is 30 days). For that many
          days the number you provided will receive daily messages at around 2PM
          PST. Each message will detail an influential LGBT person in history
          and where to learn more.
        </p>
      </div>
    );
  }
}
