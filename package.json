{
  "name": "backend",
  "version": "1.0.0",
  "description": "A backend for the LGBT Through History Messaging Service",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "Confidential",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.18.2",
    "mailgun-js": "^0.22.0",
    "mongoose": "^5.7.12",
    "node-cron": "^2.0.3",
    "stripe": "^7.13.1",
    "uuid": "^3.3.3"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
